@model IEnumerable<TrashCollector.Models.DaysOfWeek>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DayName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DayName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DaysOfWeekId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DaysOfWeekId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DaysOfWeekId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
